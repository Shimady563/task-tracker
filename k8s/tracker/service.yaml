apiVersion: v1
kind: Service
metadata:
  name: tracker
  namespace: tracker
  labels:
    app: tracker
    component: microservice
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: tracker
    component: microservice


